use tests.test_bitmap;
use tests.test_color;
use tests.test_core_types;
use tests.test_event;
use tests.test_event_dispatch;
use tests.test_input_dev;
use tests.test_layout;
use tests.test_obj_tree;
use tests.test_pool;
use tests.test_rect;
use tests.test_style;
use tests.test_dirty_region;

test "phase0 suite loads" {
    const sentinel: i32 = TEST_BITMAP_MODULE
        + TEST_COLOR_MODULE
        + TEST_CORE_TYPES_MODULE
        + TEST_EVENT_MODULE
        + TEST_EVENT_DISPATCH_MODULE
        + TEST_INPUT_DEV_MODULE
        + TEST_LAYOUT_MODULE
        + TEST_OBJ_TREE_MODULE
        + TEST_POOL_MODULE
        + TEST_RECT_MODULE
        + TEST_STYLE_MODULE
        + TEST_DIRTY_REGION_MODULE;
    _ = sentinel;
}
